# 项目实施文档

> 目的：快速恢复开发状态，无需浏览全部代码即可上手继续开发与验证。

---

## 🗂️ 当前概览

| 项目 | 值 |
|---|---|
| 当前版本 | **v3.6.11** |
| 开发分支 | `main` |
| 上次更新 | 2026-03-09 |
| 核心功能 | 注册、心跳、日志发送（23 种类型）、白名单轮换、策略接收、任务面板 |

---

## 🚀 快速上手

```powershell
# 1. 构建
dotnet build

# 2. 本地调试：启动本地接收器（可选，用于离线验证心跳是否到达）
.\artifacts\TestReceiverPublish\TestReceiver.exe

# 3. 启动主程序
dotnet run --project src/SimulatorApp
# 或使用发布包：
.\artifacts\SimulatorAppPublish\SimulatorApp.exe
```

### 发布独立 exe

```powershell
.\scripts\publish_simulatorapp.ps1      # 主应用（约 73 MB 单文件，自动 bump 版本号 + git commit/push）
.\scripts\publish_all.ps1               # 发布全部组件
```

> 发布产物位于 `artifacts/*Publish/`，拷贝单个 exe 即可在任意 Windows x64 机器运行，无需安装 .NET。

---

## 📁 关键文件

| 文件 | 说明 |
|---|---|
| `src/SimulatorLib/Protocol/LogJsonBuilder.cs` | 所有日志类型的 JSON 构建（23 种） |
| `src/SimulatorLib/Workers/LogWorker.cs` | 日志发送逻辑（HTTPS/TCP 通道、EPS 控制、写锁） |
| `src/SimulatorLib/Workers/HeartbeatWorker.cs` | 心跳 Worker（TCP 长连接、重连、策略分发） |
| `src/SimulatorLib/Workers/HeartbeatStreamRegistry.cs` | 心跳 stream 注册表 + 每 clientId 写锁 |
| `src/SimulatorLib/Workers/PolicyReceiveWorker.cs` | 策略接收：TCP cmdId==17 触发 HTTPS 心跳拉取策略 JSON → 回 ACK；HTTPS 路径通过 `ProcessHttpsResponseBodyAsync` 共享 |
| `src/SimulatorLib/Workers/WhitelistUploadWorker.cs` | 白名单轮换上传（HTTPS multipart + clientScanStatus 通知，对齐 C++ WL 协议，15 分钟周期） |
| `src/SimulatorLib/Workers/RegistrationWorker.cs` | 批量注册，支持版本选择 |
| `src/SimulatorLib/Models/LogCategory.cs` | 日志分类枚举 + 中文名映射 |
| `src/SimulatorLib/Models/TaskRecord.cs` | 任务面板数据模型（统计字段 `long`，防亿级消息溢出） |
| `src/SimulatorLib/Persistence/ClientsPersistence.cs` | `Clients.log` 读写 |
| `src/SimulatorApp/ViewModels/MainViewModel.cs` | UI 入口，所有属性与命令 |
| `src/SimulatorApp/TcpDiagWindow.xaml.cs` | 连接诊断 + SSH 平台日志收集（时间偏差、find递归、awk过滤、/var/log/messages）|
| `src/SimulatorApp/SshLogWindow.xaml.cs` | SSH 收集进度独立浮窗（X隐藏不关闭）|

---

## 🛠️ 调试技巧

**连接诊断**：

```powershell
Test-NetConnection -ComputerName <host> -Port <port>
```

**Clients.log 异常时备份重建**：

```powershell
mv Clients.log Clients.log.bak
```

**失败日志位置**（程序运行目录下）：

| 文件 | 内容 |
|---|---|
| `logs/logsend-failures-*.log` | 日志发送失败分类统计（≤ 50 MB） |
| `logs/logsend-failures-https-*.log` | HTTPS 失败详情（含状态码和响应体） |
| `logs/heartbeat_monitor_*.log` | 心跳断线原因（6 种：服务端关闭/写失败/连接失败/超时/Session 超时/锁竞争跳过） |
**SSH 平台日志收集（`TcpDiagWindow` → "SSH 日志收集" 区）**：

1. 填入平台 SSH 地址/端口/账号/密码，点 **"收集日志"**
2. 程序自动：RTT 偏差检测 → `su root` → `find /root/logs -newer .ts_marker`（递归）→ 小文件直接 SFTP、大文件服务端 `awk` 时间戳过滤 → 收集 `/var/log/messages`
3. 单个文件失败自动跳过（`⚠ xxx 跳过: ...`），不影响其他文件
4. 输出目录：`debug_collect_yyyyMMdd_HHmmss/`（程序工作目录下）

| 输出文件 | 来源 |
|---|---|
| `<subdir>/*.log` | `/root/logs/**`（保留子目录结构）|
| `var_log_messages` | `/var/log/messages`（内核级 socket/fd 错误）|
| `heartbeat_monitor_*.log` | 本地心跳断线日志 |
| `logs/*.log` | 本地 logsend / logsend-failures / logsend-https 等 |
| `Clients.log` | 注册结果快照（不按时间过滤）|
**日志发送协议路由**：

| 通道 | 日志类型 |
|---|---|
| TCP 长连接（CMDID=21） | 威胁检测 5 种子类（复用心跳 stream，需持写锁） |
| HTTPS `/USM/docGuardLog.do` | DP（文件保护，EDR 专属） |
| HTTPS `/USM/sysGuardLog.do` | SysGuard（系统防护，EDR 专属） |
| HTTPS `/USM/clientAlertlog.do` | 进程控制、病毒、漏洞等通用类 |
| HTTPS `/USM/clientULog.do` | 外设控制（9 种子类，UsbType 字段区分） |
| HTTPS `/USM/hotplugDevLog.do`（CMDID=204）| U盘插拔（CMDVER=1，CMDContent）/ 网口Up/Down（CMDVER=4，CMDContentOtherDev + OtherDevType=7）|

---

## 🧪 TestReceiver 本地调试接收器

**用途**：在没有真实平台时验证心跳包/日志包能否正确发出。

```powershell
# 启动接收器（监听 TCP 8441，UDP 4565）
.\artifacts\TestReceiverPublish\TestReceiver.exe

# 在另一个终端启动 Runner 向接收器发送
.\artifacts\SimulatorRunnerPublish\SimulatorRunner.exe
```

> 若出现 IPv6/IPv4 不匹配，平台地址请使用 `127.0.0.1` 而非 `localhost`。

---

## 📋 待办

1. **Win2012R2 兼容性**：见 `feature/win2012r2-compat` 分支，尚未合并。
2. **策略字段覆盖率验证**：对照平台后端期望的全部 JSON 字段逐一确认，确保 ACK 回包被平台识别。
3. **任务面板 UI 优化**：列宽自适应 / 支持导出 CSV。
5. ~~**⚠️ 待验证：3000 客户端离线问题未彻底解决**~~ ✅ 已修复（v3.3.0，见下方调查记录）
6. 🔴 **[BUG-2026-001] 2000 客户端心跳不稳定 + 日志发送失败** → [GitHub Issue #4](https://github.com/RogerXie314/IEGPerTest/issues/4) | [`本地文档`](bugs/BUG-2026-001-2000客户端心跳稳定性.md)

---

## � Bug 跟踪

| BugID | 标题 | 状态 | 文件 |
|---|---|---|---|
| [BUG-2026-001 #4](https://github.com/RogerXie314/IEGPerTest/issues/4) | 2000 客户端心跳不稳定 + 日志发送失败 | � 根因已确认（等待平台方修复） | `HeartbeatWorker` / `LogWorker` |

---

## �🔬 问题调查记录

### ✅ 已解决：心跳离线但日志仍在发（2026-03-04）

**现象**：100台×13条/s 运行一段时间后，心跳全部离线，但平台后台日志(cmdId:200)还在持续流入。包括完全没有发日志、只开心跳的另外两台机器也全部离线。

**根本原因**：心跳写锁超时后错误地 `Dispose` 了仍然有效的 TCP 连接，触发重连雪崩。

雪崩链路：
```
LogWorker 高速写 → WriteAsync 因平台繁忙阻塞 → HeartbeatWorker 等锁超时
  → lockAcq=false → throw TimeoutException（旧代码）
  → 外层 catch 误判为写失败 → tcp.Dispose()（TCP 其实没坏！）
  → HeartbeatWorker 重连 → LogWorker 降级建独立 TCP
  → 平台连接槽(ulimit=1024)被独立 TCP 占满
  → 所有机器 HeartbeatWorker 重连失败 → 全部永久离线
  → 但 LogWorker 独立 TCP 继续发日志 → 平台看到日志来但心跳消失
```

**修复**（v3.3.0）：`lockAcq=false` 时跳过本次心跳并 `continue`，不 Dispose TCP。详见变更记录。

---

## 📝 变更记录

### v3.6.0 — 2026-03-07：TCP 发送可靠性修复（对齐老工具行为）

**背景**：实测发现 C# 工具在 1000 客户端 + 1 EPS 场景下日志发送能力弱于老工具（C++），深度代码对比发现 6 处行为偏差。

**修复内容：**

1. **\[P0\] `write_ex` 触发 Unregister**（`LogWorker.SendLogTcpLikeExternalAsync`）
   - 旧：`WriteAsync` thrown 后返回 `write_ex:*`，stream 保留在注册表；心跳下一次写入被污染的半截 stream → 平台断连 → 最多要等 90s SessionStale 才能重连。
   - 新：catch 块中立即 `_streamRegistry.Unregister(clientId)`，HeartbeatWorker 的 drain 任务收到 `n==0` 后在 1~2 个心跳周期内完成重连。

2. **\[P0\] `allThreatTcp` 失败后立即 break**（`LogWorker.StartCoreAsync`）
   - 旧：5 种类型逐一发送，任意一种 `stream_null` 后仍继续发剩余类型并等待 50ms×N，导致失败计数 ×5 倍膨胀，并浪费 200ms。
   - 新：对齐 C++ `goto END`，任一类型失败后立即 `break`，剩余类型不再发送。

3. **\[P1\] LockBusy 时补发心跳而非跳过**（`HeartbeatWorker`）
   - 旧：4000ms 等锁超时后直接 `continue`，整个 30s 周期内少发 1 次心跳（实际变为 60s 无心跳）。
   - 新：等 200ms 后再重试获取锁（500ms 超时），锁成功则立即补发；仍获取失败才跳过本次，且不等完整 intervalMs。

4. **\[P1\] SessionStale 重连加随机 jitter**（`HeartbeatWorker`）
   - 旧：270 个客户端同时触发 SessionStale 后同时发起 `ConnectAsync`，冲击平台 → 更多拒绝 → 雪崩。
   - 新：重连前加 `Random.Shared.Next(0, 5000)` ms 随机延迟，打散连接潮。

5. **\[P3\] drain 异常记录具体类型**（`HeartbeatWorker`）
   - 旧：所有 drain 异常统一记为 `reason=连接异常`，无法区分网络错误/正常 Dispose/取消。
   - 新：`catch (Exception exDrain)` 记录 `exDrain.GetType().Name`；`ObjectDisposedException` 单独映射为 `连接已释放`。

6. **\[P3\] `evtDisconnAtMs[]` 改用 `Interlocked.Exchange`**（`HeartbeatWorker`）
   - 旧：`long` 类型在 32-bit 运行时不保证原子写；drain Task 和主循环双写同一元素存在数据竞争。
   - 新：所有写入点（PolicyDrain / RawDrain / drain catch / ConnTimeout / ConnFailed / WriteFailed / SessionStale）均改为 `Interlocked.Exchange`。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Workers/LogWorker.cs` | P0×2：write_ex Unregister；allThreatTcp break |
| `src/SimulatorLib/Workers/HeartbeatWorker.cs` | P1×2：LockBusy 补发；SessionStale jitter；P3×2：drain 异常类型；Interlocked |

---

### v3.6.1 — 2026-03-08：修复 stream_null 永久空缺（压测日志分析发现）

**根因**：1 天压测日志分析发现，TCP 风暴期间 LogWorker 写入超时 → `write_ex:OperationCanceledException` → 调用 `Unregister`；但此时 HeartbeatWorker 的 TCP 仍然存活，`if (tcp == null || !alive)` 为 false，永远不走重连分支，因此也永远不再执行 `Register`。注册表空缺持续至该 TCP 自然死亡（约 5 小时），期间所有日志静默丢弃。

**现象**：平台看到心跳全部在线，但 200 客户端中约 60 个日志静默，EPS 从期望 1000 降至 ~700，持续 5 小时（`stream_null` 586,266 次，~34.5 次/秒，`lock_timeout` 2,181 次）。

**修复**：在 HeartbeatWorker `WriteAsync + FlushAsync` 成功后、`ReleaseWriteLock` 之前调用 `_streamRegistry?.Register(c.ClientId, stream!)`。
- 位于**写锁内**：确保 LogWorker 等锁方在锁释放后能立即 `TryGet` 成功，消除 ReleaseWriteLock 与 Register 之间的竞态窗口
- **幂等**：`ConcurrentDictionary[key] = value`，不影响正常连接流程  
- 最迟下一心跳周期（≤30s）恢复，而非等待数小时 TCP 自然死亡

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Workers/HeartbeatWorker.cs` | 心跳写成功后幂等重注册 stream（锁内，消除竞态） |

---

### v3.6.10 — 2026-03-09：修复发布脚本文档自动更新

`publish_simulatorapp.ps1` 文档更新历经多次尝试均失败，根因是：

1. `git ls-files` 对中文文件名默认八进制转义（`core.quotepath=true`），导致路径含非法字符，`Test-Path` 报错
2. `git add $docRelPath` 传入乱码路径，`fatal: pathspec did not match any files`

**修复**：彻底放弃 `git ls-files` 路径获取方案，改为用 `[char]0xXXXX` 逐字拼接中文文件名构造绝对路径，`[System.IO.File]::Exists()` 替换 `Test-Path`，`git add docs/` 替换按路径 add，彻底绕开 PowerShell 控制台编码问题。

| 文件 | 变更 |
|---|---|
| `scripts/publish_simulatorapp.ps1` | 文档路径构造方式、git add 方式 |

---

### v3.6.9 — 2026-03-09：UI 清理：移除插拔&网口事件分组协议噪音

将"插拔 & 网口事件（IEG）"分组的 UI 文字去除协议实现细节，保留语义名称，符合用户界面表达规范。

1. 分组标题移除 `— 共用 CMDID=204（hotplugDevLog），CMDVER 区分` 后缀
2. CheckBox `U盘插拔 (CMDVER=1)` → `U盘插拔`
3. CheckBox `网口Up/Down (CMDVER=4)` → `网口Up/Down`
4. 删除底部 JSON 字段注释行（`U盘插拔：CMDContent；网口事件：CMDContentOtherDev + OtherDevType=7`）

| 文件 | 变更 |
|---|---|
| `src/SimulatorApp/MainWindow.xaml` | 插拔&网口事件分组标题、CheckBox 文字、底部注释行 |

---

### v3.6.8 — 2026-03-09：修复任务完成后无法再次添加任务

**Bug**：任务（日志发送 / 白名单上传）自然跑完后，`_logCts` / `_uploadCts` 未被重置，防重入守卫 `_logCts != null && !IsCancellationRequested` 误判为"仍在运行"，导致点击【添加任务】永远提示"任务已在运行"，只能点【结束任务】触发 `Cancel()` 才能解锁。

**修复**：在两个 `Task.Run` 的 `finally` 块中增加 `_logCts?.Dispose(); _logCts = null`（白名单同理），任务自然结束后立即释放。

| 文件 | 变更 |
|---|---|
| `src/SimulatorApp/ViewModels/MainViewModel.cs` | 日志发送 finally + 白名单上传 finally 各增加 CTS 置 null |

---

### v3.6.7 — 2026-03-09：修正 USB访问告警 OperationContent

1. **`BuildUsbWarningLog` 参数 `operation` → `operationContent`（int）**：原 `operation: "Copy"` 字符串参数从未写入 JSON（dead param），方法体原先写死 `OperationContent = 1` 且注释错为"读取"。
2. **OperationContent 随机循环**：改为 `(messageIndex % 6) + 1`，每条消息依次产生 1=删除 / 2=重命名 / 3=修改 / 4=读 / 5=新建 / 6=执行，对齐 IEG 源码注释（`WLJsonParse.cpp` L803）。
3. **修正 U盘告警(老版本) help 描述**：原描述"CDROM/WiFi/蓝牙/串口等被策略禁用时产生"不准确，实际代码只发 UsbType=2 固定值，已改为准确说明。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Protocol/LogJsonBuilder.cs` | 参数 `operation` → `operationContent(int)`，写入 JSON |
| `src/SimulatorLib/Workers/LogWorker.cs` | 传 `(messageIndex % 6) + 1` |
| `src/SimulatorApp/LogCategoryHelpWindow.xaml` | 修正 U盘告警(老版本) 描述 |

---

### v3.6.6 — 2026-03-09：标注"U盘告警(老版本)"

重命名为"U盘告警(老版本)"，明确标注该类型仅用于模拟旧版 IEG 客户端（UsbType=2）。当前版本 IEG 源码的 `ExtDevLog_GetJsonByVector` 中 UsbType 从 4 开始（4=CDROM, 5=wifi, 6=蓝牙…），不产生 UsbType=2 的日志，该类型仅用于向后兼容性验证。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Models/LogCategory.cs` | display + parse name |
| `src/SimulatorApp/MainWindow.xaml` | checkbox 标签 |
| `src/SimulatorApp/ViewModels/MainViewModel.cs` | `GetSelectedCategories()` |
| `src/SimulatorApp/LogCategoryHelpWindow.xaml` | 说明文字 |

---

### v3.6.5 — 2026-03-09：重命名"USB设备" + 清理死参数

1. **`LogCategory.Usb` 显示名改为"U盘告警"**：原名"USB设备"易与"U盘插拔"混淆，实际含义是旧版通用禁用告警（UsbType=2，接口 `/USM/clientULog.do`），与物理插拔事件无关。
2. **移除 `BuildUsbDeviceLog` 的无用 `state` 参数**：该参数从未写入 JSON 字段，属于死代码，同步修正 LogWorker 中所有调用点（ExtDev 块、`Usb` 通用块）。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Models/LogCategory.cs` | "USB设备" → "U盘告警" (display + parse name) |
| `src/SimulatorApp/MainWindow.xaml` | checkbox 标签 |
| `src/SimulatorApp/ViewModels/MainViewModel.cs` | `GetSelectedCategories()` |
| `src/SimulatorApp/LogCategoryHelpWindow.xaml` | 说明文字（6 处）|
| `src/SimulatorLib/Protocol/LogJsonBuilder.cs` | 移除 `state` 参数 |
| `src/SimulatorLib/Workers/LogWorker.cs` | 移除所有 `state:` 传参 |

---

### v3.6.4 — 2026-03-09：修正外设/网口 LogContent 描述格式 + 脚本清理

**背景**：发现外设控制日志的 `LogContent` 字段被错误填为 `"禁蓝牙-event-17"` 形式的拼凑字符串，与 IEG 源码（`WLJsonParse.cpp` 注释）中的标准格式不符。

**修复内容：**

1. **`GetExtDevLogContent()` 新增**（`LogCategory.cs`）：返回标准描述字符串。
   ```
   蓝牙       → "蓝牙使用被禁止"
   CDROM      → "CDROM使用被禁止"
   wifi       → "wifi使用被禁止"
   USB 接口   → "USB接口使用被禁止"
   移动设备   → "移动设备使用被禁止"
   串口/并口   → "串口/并口使用被禁止"
   ```
2. **`BuildUsbDeviceLog()` 参数修正**（`LogJsonBuilder.cs`）：`deviceName` → `logContent`，新增 `userName` 参数（对齐 IEG 协议字段）。
3. **`LogWorker.cs` 调用点全部修正**：
   - ExtDev 9 种子类统一使用 `GetExtDevLogContent()`
   - 通用 `Usb` 类型改为固定 `"U盘使用被禁止"`
   - `NetAdapterEvent` DOWN 事件 IP 改为 `"0.0.0.0"`（IP 已释放，更贴近真实）
4. **脚本清理**：删除 10 个过时脚本（`analyze_failures.py`、`build_and_run.ps1`、`extract_*.py`、`search_*.py`、`summarize_*.py`、`verify_logtype_fix.ps1`）。
5. **`publish_simulatorapp.ps1` 增强**：自动读取 csproj 版本 → 递增 patch 号 → 写回 → 构建 → `git commit + push`，无需手动修改版本号。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Models/LogCategory.cs` | 新增 `GetExtDevLogContent()` |
| `src/SimulatorLib/Protocol/LogJsonBuilder.cs` | `BuildUsbDeviceLog` 参数重命名/新增 |
| `src/SimulatorLib/Workers/LogWorker.cs` | 所有外设/USB/网口 LogContent 修正 |
| `scripts/publish_simulatorapp.ps1` | 自动 bump patch + git commit/push |
| `scripts/` | 删除 10 个过时脚本 |

---

### v3.6.3 — 2026-03-09：新增网口 Up/Down 日志类型

**背景**：IEG 平台支持网络适配器插拔事件（`WLNetAdapterEvent.h`），模拟器此前未实现，补充该类型完成全日志覆盖。

**协议说明**：
- 接口：`/USM/hotplugDevLog.do`（CMDID=204，与 U盘插拔共用）
- 区别：`CMDVER=4`（U盘=1），数据放在 `CMDContentOtherDev` 数组，`OtherDevType=7`（固定）
- `PlugEvent` 值：1=UP状态轮询, 2=UP变化触发, 3=DOWN状态轮询, 4=DOWN变化触发
- DOWN 事件时 IP 字段填 `"0.0.0.0"`（地址已释放）

**UI 设计**：将 U盘插拔 从主 WrapPanel 提取，与新增的"网口Up/Down"共同放入带边框分组"插拔 & 网口事件（IEG）"，标注 CMDID/CMDVER 区分关系，Checkbox 颜色沿用 DarkBlue（IEG专属）。

**Help 弹窗更新**：卡片③标题改为"USB / 网口：插拔相关分类说明"，新增网口Up/Down协议说明行及场景E（网口管控验证）。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Models/LogCategory.cs` | 新增 `NetAdapterEvent = 29`，含 display name / parse name |
| `src/SimulatorLib/Protocol/LogJsonBuilder.cs` | 新增 `EnvelopeNetAdapter()`、`BuildNetAdapterLog()` |
| `src/SimulatorLib/Workers/LogWorker.cs` | dispatch switch 新增 `NetAdapterEvent` case |
| `src/SimulatorApp/MainWindow.xaml` | 新增"插拔 & 网口事件"分组 Border |
| `src/SimulatorApp/ViewModels/MainViewModel.cs` | 新增 `CatNetAdapterEvent` binding + `GetSelectedCategories` entry |
| `src/SimulatorApp/LogCategoryHelpWindow.xaml` | 完整更新文档：新增协议行、场景E、颜色说明 |

---

### v3.6.2 — 2026-03-09：新增日志分类说明弹窗

**背景**：日志分类模块含多个相似名称（USB设备 / USB访问告警 / U盘插拔 / 外设控制子类），颜色区分（黑/蓝/绿）含义对新用户不够直观，导致测试时错误勾选，影响压测结论。

**变更**：在主界面"项目类型"行右侧新增 **「？分类说明」** 按钮，点击弹出 `LogCategoryHelpWindow` 子页面，内容包含：

1. **项目类型说明**：IEG（HTTPS 短连接 ≤100 EPS）vs EDR（TCP 长连接最高 6000 EPS）的适用场景。
2. **字体颜色含义**：黑色=通用、深蓝=IEG 专属、深绿=EDR 专属。
3. **USB 相关四类区分**：USB设备（旧版通用 UsbType=2）/ 外设控制9子类（精确 UsbType，IEG 新版）/ USB访问告警（违规文件操作）/ U盘插拔（物理插拔事件），含接口路径对比与耦合关系说明。
4. **典型场景建议**：IEG 综合压测 / EDR 威胁检测压测 / U盘管控全链路验证 / 老版本兼容测试 四种场景的勾选推荐。

| 文件 | 变更 |
|---|---|
| `src/SimulatorApp/LogCategoryHelpWindow.xaml` | 新增说明弹窗 UI |
| `src/SimulatorApp/LogCategoryHelpWindow.xaml.cs` | 新增弹窗 Code-behind |
| `src/SimulatorApp/MainWindow.xaml` | 项目类型行新增「？分类说明」按钮 |
| `src/SimulatorApp/MainWindow.xaml.cs` | 新增 `ShowLogCategoryHelp_Click` 事件处理 |

---

### v3.5.0 — 2026-03-07：协议对齐修复（白名单上传 / 策略接收 / 数值溢出）

**修复内容：**

1. **统计字段溢出修复**：`MainViewModel` / `TaskRecord` 中的成功/失败计数字段由 `int` 改为 `long`，消除亿级消息场景下的数值溢出。

2. **WhitelistUploadWorker 协议对齐**（对齐 C++ `Send_FileLog_WL_ToServer`）：
   - 上传前 POST `/USM/clientScanStatus.do` 通知 `SolidifyStatus=11`（WL_SOLIDIFY_UPLOAD）
   - 文件通过 HTTPS multipart/form-data 上传至 `/USM/upLoadSysWhiteFile.do?id=<b64url>&filepath=<b64url>`，字段名 `"file"`
   - 上传成功后再次通知 `SolidifyStatus=10`（WL_SOLIDIFY_UPDATE）
   - 每批共享一个 `HttpClient`，忽略平台自签名证书

3. **PolicyReceiveWorker 协议对齐**（对齐 C++ `WLServerTestDlg` 策略流程）：
   - TCP 心跳响应 `cmdId==1`（HEARTBEAT_CMD_BACK）：正常 ACK，忽略
   - TCP 心跳响应 `cmdId==17`（HEARTBEAT_CMD_POLCY）：主动发 HTTPS 心跳 `/USM/clientHeartbeat.do`，从响应体解析策略 JSON，再 POST `/USM/clientResult.do` 回报
   - 新增 `ProcessHttpsResponseBodyAsync()`，供 HTTPS 心跳路径（Linux 模式）复用同一策略解析逻辑

4. **HeartbeatWorker**：HTTPS 心跳成功后将响应体转发给 `PolicyReceiveWorker.ProcessHttpsResponseBodyAsync`，两条路径均能触发策略接收。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Workers/WhitelistUploadWorker.cs` | 重写为 HTTPS multipart 上传 + clientScanStatus 双向通知 |
| `src/SimulatorLib/Workers/PolicyReceiveWorker.cs` | cmdId==17 → HTTPS 触发；新增 `ProcessHttpsResponseBodyAsync` |
| `src/SimulatorLib/Workers/HeartbeatWorker.cs` | HTTPS 心跳响应体转发给 PolicyReceiveWorker |
| `src/SimulatorApp/ViewModels/MainViewModel.cs` | 统计字段 `int` → `long` |
| `src/SimulatorLib/Models/TaskRecord.cs` | 统计字段 `int` → `long` |

---

### v3.4.1 — 2026-03-06：威胁检测 TCP 日志对齐 C++ 批发模式

**变更内容：**

1. **`allThreatTcp` 批发模式**：当全部选中分类均为威胁检测 TCP 时，`LogWorker` 每 tick 依次发送全部 N 种（类型间 ~50ms 间隔），完全对齐 C++ `SendThreatLog_ToserverTCP`（File→Sleep50→ProcStart→Sleep50→Reg→…）。混合场景保留原有按 `msgIdx` 轮转逻辑。
2. **进度计数修正**：`allThreatTcp` 模式下 `totalMessages × N`，进度百分比与平台实际记录条数对齐。
3. **UI / 默认值**：威胁 TCP EPS 标签改为"**轮/秒**"；默认值改为 1 轮/秒；EDR 预设含全 5 种（新增 OsEvent）。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Workers/LogWorker.cs` | `allThreatTcp` 批发模式；混合场景保留原逻辑 |
| `src/SimulatorApp/ViewModels/MainViewModel.cs` | 威胁默认 EPS=1 轮/秒；EDR 预设含 OsEvent |
| `src/SimulatorApp/MainWindow.xaml` | 威胁 TCP EPS 标签改为"轮/秒" |

---

### v3.4.0 — 2026-03-04：SSH 诊断增强 + OS 类型切换

**新增功能：**

1. **客户端 OS 类型切换**：MainWindow OS 面板移至服务器设置右侧（Col1 Row0），可选 Windows / Linux，影响注册包 OS 字段。

2. **SSH 平台日志一键收集**（`TcpDiagWindow` 增强）：
   - SSH → `echo PASSWORD | su root -c '...'` 切换 root 权限，`whoami` 验证
   - `date +%s` RTT 半程补偿，截止时间自动修正平台时钟偏差
   - `find /root/logs -newer .ts_marker`（去掉 `-maxdepth 1`，递归搜索）
   - `relPath` 保留子目录层级，`/tmp` 镜像与本地输出结构一致
   - 大文件服务端 `awk` 时间戳过滤（格式 `[YYYY-MM-DD HH:MM:SS:ms]`）
   - 额外收集 `/var/log/messages`（系统级 socket 拒绝、`too many open files`）
   - **单个文件 try-catch 隔离**：单文件失败打印 `⚠ xxx 跳过` 后继续

3. **本地日志类型补全**：`logs/*.log`（含 logsend-failures / logsend-https）→ 存入 `logs/` 子目录；新增 `Clients.log` 快照。

4. **SshLogWindow 独立浮窗**：SSH 收集进度独立展示，X 键隐藏不关闭。

5. **DateTimeOffset 构造异常修复**：`new DateTimeOffset(dt, TimeSpan.Zero)` → `new DateTimeOffset(dt)`，避免 `Kind=Local` 时抛 `ArgumentException`。

| 文件 | 变更 |
|---|---|
| `src/SimulatorApp/MainWindow.xaml` | OS 面板移至服务器设置右侧 |
| `src/SimulatorApp/SshLogWindow.xaml/.cs` | 新增 SSH 收集进度独立浮窗 |
| `src/SimulatorApp/TcpDiagWindow.xaml.cs` | su root、find递归、relPath、awk过滤、/var/log/messages、per-file try-catch |

---

### v3.3.0 — 2026-03-04：修复锁超时导致心跳 TCP 被错误销毁

**问题**：`HeartbeatWorker` 在写锁等待超时（`lockAcq=false`）时，原代码抛出 `TimeoutException` 被外层 `catch` 误判为 TCP 写入失败，执行 `tcp.Dispose()` 销毁了仍然有效的 TCP 连接。后续 HeartbeatWorker 重连因平台连接槽被 LogWorker 降级独立 TCP 占满而失败，最终所有客户端心跳离线，但日志还在持续发送。

**触发场景**：多台机器同时高速打日志（如 3台 × 100客户端 × 13条/s），平台侧繁忙导致 `WriteAsync` 阻塞，`sendCts`（5s总预算）耗尽后 `AcquireWriteLockAsync` 返回 `false`。

**雪崩链路**：`锁超时 → tcp.Dispose() → HeartbeatWorker重连 → LogWorker降级独立TCP → 平台连接槽满 → 重连永久失败 → 心跳全离线但日志仍在发`

**修复**：`lockAcq=false` 时跳过本次心跳（`continue`），不 Dispose TCP。新增 `Reason.LockBusy = 6`，不计入离线数，在监控日志和 UI 中显示为 `⚡锁竞争跳过:N`。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Workers/HeartbeatWorker.cs` | `lockAcq=false` 时跳过心跳而非 Dispose TCP；新增 `Reason.LockBusy`；统计/上报加入 LockBusy 计数 |
| `src/SimulatorApp/ViewModels/MainViewModel.cs` | UI 日志显示 `⚡锁竞争跳过:N`；`HeartbeatStats` 新增 `RsnLockBusy` 字段 |

---

### v3.2.0 — 2026-03-03：NetworkStream 写锁

**问题**：v3.1.0 复用心跳 TCP stream 后，`HeartbeatWorker`（定时写心跳）与 `LogWorker`（按 EPS 写日志）**并发 `WriteAsync` 同一 `NetworkStream`**，导致：
- PT 包体字节互相穿插 → 平台主动断连 → **3000 客户端全部离线**
- 策略回包损坏 → 平台策略审计始终显示 **"待发送"**

**修复**：`HeartbeatStreamRegistry` 为每个 clientId 维护 `SemaphoreSlim(1,1)` 写锁，心跳写与日志写串行化。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Workers/HeartbeatStreamRegistry.cs` | 新增 `_writeLocks`、`AcquireWriteLockAsync`、`ReleaseWriteLock` |
| `src/SimulatorLib/Workers/HeartbeatWorker.cs` | 心跳 `WriteAsync` 前获取写锁，finally 释放 |
| `src/SimulatorLib/Workers/LogWorker.cs` | 共享 stream 写入前获取写锁，重取最新 stream，finally 释放 |

---

### v3.1.0 — 2026-03-03：日志发送复用心跳 TCP 流

**问题**：日志发送为每客户端另开独立 TCP 连接，导致同一 ComputerID 存在两条连接，平台踢掉后发者。

**修复**：引入 `HeartbeatStreamRegistry`，`LogWorker` 优先复用心跳 stream，降级才走独立连接。

| 文件 | 变更 |
|---|---|
| `src/SimulatorLib/Workers/HeartbeatStreamRegistry.cs` | 新增（stream 注册表） |
| `src/SimulatorLib/Workers/LogWorker.cs` | 复用心跳 TCP 流、任务面板集成 |
| `src/SimulatorLib/Workers/HeartbeatWorker.cs` | 回包统计修复（每次 ReadAsync 立即更新时间戳）、TCP 重连对齐原始 2s 超时 |

---

### v3.0.0 — 2026-03-03：任务面板 + 策略接收 + 威胁检测子类

1. **威胁检测拆分为 5 种事件子类**（TCP 长连接，CMDID=21，支持 6000 EPS）：`ThreatRegAccess`、`ThreatFileAccess`、`ThreatOsEvent`、`ThreatDllLoad`
2. **任务面板**：`TaskRecord` 模型 + DataGrid，所有后台任务实时显示状态/进度/成功/失败
3. **策略接收**：`PolicyReceiveWorker` 在心跳 TCP 流上监听下发包，解析 CMDTYPE/CMDID，HTTPS 回 ACK
4. **白名单轮换重写**：`RunRotatingAsync`，随机轮换，15 分钟周期，TaskRecord 集成
5. **注册版本选择**：ComboBox 选择客户端版本（默认 `V300R011C01B030`）
6. **分通道独立配置**：HTTPS 与威胁检测各自独立配置客户端数和 EPS
7. **失败日志按文件大小（50 MB）限制**，移除硬编码条数上限

| 新增文件 | 说明 |
|---|---|
| `src/SimulatorLib/Models/TaskRecord.cs` | 任务面板数据模型 |
| `src/SimulatorLib/Workers/PolicyReceiveWorker.cs` | 策略接收 Worker |

---

### v2.4.0 — 2026-03-02：多IP连接计数 + TCP 参数诊断窗口

1. **多IP运行时连接计数**：`LogWorker` 用 `Interlocked` 原子统计每IP实际建连次数，任务结束后输出分布
2. **TcpDiagWindow 参数优化区**：
   - ⚡ 一键推荐（端口 1025–65535 + TIME_WAIT=30s，UAC 提权）
   - `netsh show dynamicport` 正则解析，中英文系统均兼容
   - 应用后延迟 600ms 刷新，避免传播延迟

---

### v2.3.0 及以前

| 版本 | 主要变更 |
|---|---|
| v2.3.0 | 外设控制 9 种子类、EPS 精确控制（deadline 模式）、防 TLS 握手风暴、压测模式（3000 客户端 / 6000 EPS）|
| v2.2.0 | UI 布局优化（2×2 统计面板）、心跳断线原因监控日志 |
| v2.1.0 | 11 种日志类型完整实现、`LogTypeMapper`（Type/SubType 映射） |
| v2.0.0 | WPF UI 全面重构、单文件发布、持久化 `config.json` / `Clients.log` |
| v1.0.0 | 基础原型（注册、心跳、日志发送、白名单上传可运行） |