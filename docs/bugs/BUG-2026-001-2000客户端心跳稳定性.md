# BUG-2026-001：2000 客户端心跳 + 日志发送失败

| 字段 | 值 |
|---|---|
| **BugID** | BUG-2026-001 |
| **状态** | 🔍 调查中（Step 2 完成，进入 Step 3 平台日志） |
| **优先级** | P0（阻塞压测） |
| **发现日期** | 2026-03-05 |
| **模块** | `HeartbeatWorker` / `LogWorker` / `HeartbeatStreamRegistry` |
| **关联 commit** | `f58b609`（revert 起点）、`26ae2a2`（日志格式修正） |

---

## 现象

- 启动 2000 个客户端，心跳 + 日志，大量客户端离线，同时 UI 显示日志发送失败
- 老工具（WLServerTest C++）同等配置可稳定运行

> ⚠️ 注：「纯 HB 离线」为文档错误描述，已勘误。实测纯 HB 运行 30 分钟完全稳定。

---

## 背景 & 架构对比

### 老工具（WLServerTest，C++）

```
HBClientCount_InEveryThread = 10
→ 2000 客户端 = ~200 个 OS 阻塞线程
→ 每线程: connect → 发 HB → recv HB/LogACK → Sleep(30000)
→ 日志线程: 复用同一 g_sock[i]，3 次 SendData + Sleep(50)，不 recv
→ 日志 ACK 由 HB 线程的 RecvHeartBeatBack_TCP 的 else 分支消费
→ Sleep(30000) 精准，OS 级强保证
```

### 当前 C# 实现

```
每客户端 1 个 HeartbeatWorker Task + 1 个 DrainTask + 1 个 LogWorker Task
→ 2000 客户端 = 6000+ 并发 Task，共用 .NET 线程池（默认 ~core 数）
→ HeartbeatWorker: Task.Delay(30s)，线程池有压力时有抖动
→ LogWorker: AcquireWriteLockAsync(200ms) + WriteAsync → FlushAsync，不读 ACK
→ DrainTask: Task.Run() 无限 ReadAsync，消费所有入方向字节
```

---

## 根因假说（待验证）

### ~~假说 A：Task 调度抖动导致 HB 超时~~ ❌ 已排除
- 2026-03-05 实测：2000 客户端纯 HB 运行 **30 分钟**，全程稳定无掉线
- Task.Delay(30s) 调度精度在此规模下不是问题

### ~~假说 B：日志写操作干扰 HB stream~~ ❌ 已排除
- 2026-03-05 Step 2 实测：test3- 机器**仅运行心跳**（无日志任务），却在 16:40:46 同时出现「平台回包:0 / 服务端关闭」
- 机器1(test2-)、机器2(xrg-)、机器3(test3-) 三台机器在 **16:40 前后同时**被平台断连
- 断连原因均为服务端主动行为：「平台断session」「服务端关闭」
- 结论：问题**根源在平台侧**，与客户端写锁无关

### 假说 C：平台连接数/处理能力上限 ⭐ 当前主要嫌疑
- 2000 个 TCP 长连接 + 2000 EPS 日志流量，触发平台 fd 上限 / backlog / 内部进程异常
- **证据**：加 1 EPS 日志后平台主动断所有客户端 session（含纯HB机器），与连接数/负载上限行为吻合
- **验证方法**：SSH 收集平台机器 `/var/log/messages`，看 `too many open files` / `connection refused` / `OOM` / 守护进程重启日志

---

## 已排查 / 已做的修改

| 日期 | commit | 内容 |
|---|---|---|
| 2026-03-05 | `f58b609` | revert `6ddf31d`（独立 TCP 日志路径 WRONG） |
| 2026-03-05 | `26ae2a2` | 对齐老工具威胁检测 EventType、字段前缀、模拟数据 |

### `6ddf31d` 为何是错的（已 revert）
之前误判老工具用独立短连接发日志，实际老工具：
- `g_sock[i]` = HB 建立的持久 socket，日志线程复用它
- 日志线程**不 recv**，ACK 由 HB 线程的 `else` 分支消费
- C# 当前实现已对齐此行为（`LogWorker` 不 recv，DrainTask 消费入方向）

---

## 待验证步骤

- [x] **Step 1**：2000 客户端，仅心跳，运行 30 分钟 → **全程稳定，无掉线**（2026-03-05）

- [x] **Step 2**：2000 客户端，心跳 + 1 EPS（威胁检测TCP），运行约 5 分钟（2026-03-05）
  - test2-：日志失败 1458，平台回包 0/1000，离线原因：平台断session
  - xrg-：日志失败 4161，平台回包 0/1000，离线原因：平台断session / 服务端关闭  
  - test3-（**纯HB对照**）：无日志任务，同时段也出现平台回包:0 / 服务端关闭
  - **结论：问题在平台侧，假说B排除，假说C为主要嫌疑**

- [ ] **Step 3**：（当前步骤）SSH 收集平台机器日志，检验 `/var/log/messages` 是否有 `too many open files` / OOM / 进程重启 等迹象

---

## 修复路线（待定）

### ~~路线 A：HeartbeatWorker Task 批量化~~ — 已排除（Step 1 验证 HB 本身稳定）

### ~~路线 B：拉长写锁超时~~ — 已排除（Step 2 证明问题在平台侧）

### 路线 C：增大线程池下限
```csharp
ThreadPool.SetMinThreads(500, 500);
```
提高线程池底线，减少 Task 调度排队延迟。

---

## 修改轨迹（后续留痕）

| 日期 | 操作 | commit | 结果 |
|---|---|---|---|
| 2026-03-05 | revert 6ddf31d，对齐日志格式 | f58b609, 26ae2a2 | 构建通过 |
| 2026-03-05 | Step 1：2000 客户端纯 HB 30 分钟 | - | **稳定**，假说 A 排除 |
| 2026-03-05 | Step 2：心跳+1 EPS，三台机器同时平台断session | - | 假说B排除，**假说C主要嫌疑**，进入Step3 |
| _(待填)_ | Step 3：SSH 采集平台日志 | - | - |
| _(待填)_ | 实施修复 | - | - |

---

## 参考

- 老工具关键文件：`external/IEG_Code/code/WLServerTest/WLServerTestDlg.cpp`（L1370 HB 线程，L2152 日志线程）
- 老工具 JSON 序列化：`external/IEG_Code/code/WLUtilities/WLJsonParse.cpp`（L2326 ProcStart，L2376 Reg，L2427 File）
- 常量定义：`external/IEG_Code/code/include/ThreatLogStruct.h`
