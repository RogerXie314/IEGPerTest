<Window x:Class="SimulatorApp.Views.StreamEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑字段" Height="300" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="110"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Label Grid.Row="0" Grid.Column="0" Content="目的 MAC：" VerticalAlignment="Center"/>
      <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtDstMac" Margin="0,3"/>

      <Label Grid.Row="1" Grid.Column="0" Content="源 MAC：" VerticalAlignment="Center"/>
      <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtSrcMac" Margin="0,3"/>

      <Label Grid.Row="2" Grid.Column="0" Content="目的 IP：" VerticalAlignment="Center"/>
      <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtDstIp" Margin="0,3"/>

      <Label Grid.Row="3" Grid.Column="0" Content="源 IP：" VerticalAlignment="Center"/>
      <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtSrcIp" Margin="0,3"/>

      <Label Grid.Row="4" Grid.Column="0" Content="目的端口：" VerticalAlignment="Center"/>
      <TextBox Grid.Row="4" Grid.Column="1" x:Name="TxtDstPort" Margin="0,3"/>

      <Label Grid.Row="5" Grid.Column="0" Content="源端口：" VerticalAlignment="Center"/>
      <TextBox Grid.Row="5" Grid.Column="1" x:Name="TxtSrcPort" Margin="0,3"/>
    </Grid>

    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
      <Button Content="确定" Width="70" Margin="0,0,8,0" Click="OkButton_Click" IsDefault="True"/>
      <Button Content="取消" Width="70" Click="CancelButton_Click" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
